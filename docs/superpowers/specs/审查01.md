# 架构重构设计文档审查意见

**审查对象：** `2026-03-25-architecture-refactor-design.md`
**审查日期：** 2026-03-25
**审查人：** Claude Code

---

## 一、总体评价

设计文档结构清晰，分层合理，总体方向正确。采用分层架构解决当前扁平结构带来的问题是合适的方案。但在具体实施细节、异步处理、错误处理、测试策略等方面需要补充完善。

**评分：** ⭐⭐⭐⭐ (4/5)

---

## 二、优点

### 2.1 问题分析准确

- ✅ 准确识别了 28 个文件扁平堆放的问题
- ✅ 正确指出 3 个超大文件（>5000行）职责过重
- ✅ 识别了代码重复问题（TqApi 初始化、Excel 读写）

### 2.2 架构设计合理

- ✅ 六层架构划分符合职责分离原则
- ✅ 数据层与业务层分离，便于测试和替换数据源
- ✅ 策略层独立，支持后续扩展多种策略

### 2.3 迁移策略务实

- ✅ 分 8 个阶段迁移，风险可控
- ✅ 优先提取公共模块，逐步重构
- ✅ 保留输出文件兼容性

### 2.4 有测试规划

- ✅ 关键路径测试优先级划分合理
- ✅ 测试文件结构清晰

---

## 三、需要补充完善的内容

### 3.1 异步处理策略缺失 🔴 严重

当前系统大量使用 `asyncio`，但设计文档未说明异步架构。

**建议补充：**

```python
# data/tqsdk_client.py 中需要明确异步上下文管理
class TqSdkClient:
    async def __aenter__(self) -> 'TqSdkClient':
        self.api = self._create_api()
        return self

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        self.close()

# 业务层是否需要异步？需要明确设计
class OptionAnalyzer:
    async def analyze(self, quotes: List[OptionQuote]) -> List[AnalyzedOption]:
        # 纯计算逻辑，是否需要异步？
        pass
```

**问题：**
1. 业务层方法是否需要 `async`？
2. 如何处理 TqSDK 的异步回调？
3. 断点续传的异步状态如何管理？

### 3.2 数据模型设计不完整 🟡 中等

当前模型定义过于简单，缺少关键业务字段。

**建议补充：**

```python
@dataclass
class OptionQuote:
    # ... 现有字段 ...

    # 建议增加：
    exchange_id: str          # 交易所
    instrument_name: str      # 合约名称
    margin: float             # 保证金
    delta: Optional[float]    # Delta
    gamma: Optional[float]    # Gamma
    theta: Optional[float]    # Theta
    vega: Optional[float]     # Vega
    iv: Optional[float]       # 隐含波动率

    def is_itm(self, underlying_price: float) -> bool:
        """判断是否实值"""
        pass

    def time_to_expiry(self) -> float:
        """计算剩余时间（年）"""
        pass


@dataclass
class Position:
    """持仓模型 - 当前设计完全缺失"""
    symbol: str
    direction: str            # LONG / SHORT
    volume: int
    avg_price: float
    current_price: float
    unrealized_pnl: float
    margin: float

    def market_value(self) -> float:
        """市值"""
        pass
```

### 3.3 配置管理设计需要细化 🟡 中等

当前 `Config` 类设计过于简单，实际系统中配置项较多。

**建议补充：**

```python
# common/config.py
@dataclass
class TradingConfig:
    """交易配置"""
    max_position_per_symbol: int = 10      # 单品种最大持仓
    max_margin_usage: float = 0.8          # 最大保证金占用比例
    default_order_volume: int = 1          # 默认下单手数


@dataclass
class DataConfig:
    """数据配置"""
    quote_batch_size: int = 200            # 行情获取批次大小
    save_interval: int = 3000              # 保存间隔
    api_rebuild_interval: int = 3000       # API 重建间隔
    kline_data_length: int = 250           # K 线数据长度


@dataclass
class SchedulerConfig:
    """调度配置"""
    scheduled_times: List[tuple] = field(default_factory=lambda: [
        (20, 40), (21, 40), (22, 40), (23, 40), (0, 40), (1, 40),
        (8, 40), (9, 40), (10, 40), (12, 40), (13, 40), (14, 40), (15, 16)
    ])
    check_interval: int = 30               # 检查间隔（秒）
    cooldown_minutes: int = 5              # 冷却时间（分钟）


class Config:
    """全局配置"""
    def __init__(self, config_path: str = None):
        self.trading = TradingConfig()
        self.data = DataConfig()
        self.scheduler = SchedulerConfig()
        self._load_from_file(config_path)

    def _load_from_file(self, path: str):
        """从 YAML/JSON 文件加载配置"""
        pass
```

### 3.4 错误处理策略缺失 🔴 严重

设计中未说明错误处理机制，对于交易系统这是严重缺陷。

**建议补充：**

```python
# common/exceptions.py
class WiseCoinError(Exception):
    """基础异常"""
    pass

class DataFetchError(WiseCoinError):
    """数据获取失败"""
    pass

class OrderExecutionError(WiseCoinError):
    """订单执行失败"""
    pass

class RiskCheckError(WiseCoinError):
    """风控检查失败"""
    pass

class APIConnectionError(WiseCoinError):
    """API 连接失败"""
    pass


# common/error_handler.py
class ErrorHandler:
    """错误处理器"""

    def handle_data_error(self, error: DataFetchError) -> bool:
        """处理数据错误，返回是否重试"""
        if error.retryable:
            return self._retry_with_backoff()
        self._notify_user(error)
        return False

    def handle_trade_error(self, error: OrderExecutionError):
        """处理交易错误 - 关键！"""
        self._log_error(error)
        self._notify_user(error)
        # 订单失败需要特殊处理
```

### 3.5 日志与监控设计不足 🟡 中等

当前仅提及 `logger.py`，缺少结构化日志和监控设计。

**建议补充：**

```python
# common/logger.py
import structlog

class StructuredLogger:
    """结构化日志"""

    def __init__(self, name: str):
        self.logger = structlog.get_logger(name)

    def log_trade(self, symbol: str, action: str, price: float,
                  volume: int, result: str):
        """交易日志 - 关键操作需要结构化记录"""
        self.logger.info("trade_executed",
                        symbol=symbol, action=action,
                        price=price, volume=volume, result=result)

    def log_api_event(self, event: str, duration_ms: float = None):
        """API 事件日志"""
        self.logger.info("api_event", event=event, duration_ms=duration_ms)


# 建议：增加关键指标监控
# common/metrics.py
class Metrics:
    """关键指标"""

    def record_api_latency(self, operation: str, latency_ms: float):
        pass

    def record_order_result(self, success: bool):
        pass
```

### 3.6 测试策略需要细化 🟡 中等

当前测试设计过于简略，缺少具体测试场景。

**建议补充测试矩阵：**

| 模块 | 测试类型 | 测试场景 | 优先级 |
|------|----------|----------|--------|
| `iv_calculator.py` | 单元测试 | 平值期权 IV 计算 | 高 |
| `iv_calculator.py` | 单元测试 | 深度虚值边界处理 | 高 |
| `iv_calculator.py` | 单元测试 | 无效价格输入处理 | 高 |
| `analyzer.py` | 单元测试 | 多因子评分逻辑 | 高 |
| `analyzer.py` | 集成测试 | 完整分析流程 | 中 |
| `executor.py` | 单元测试 | 上期所平今逻辑 | 高 |
| `executor.py` | 集成测试 | 模拟下单流程 | 中 |
| `arbitrage.py` | 单元测试 | 转换套利识别 | 中 |
| `tqsdk_client.py` | 集成测试 | API 重建逻辑 | 高 |

**建议增加 Mock 策略：**

```python
# tests/conftest.py
@pytest.fixture
def mock_tqapi():
    """模拟 TqApi，不依赖真实连接"""
    with patch('tqsdk.TqApi') as mock:
        mock.return_value.get_quote.return_value = MockQuote()
        yield mock

@pytest.fixture
def sample_option_quotes():
    """测试用期权行情数据"""
    with open('tests/fixtures/option_quotes.json') as f:
        return json.load(f)
```

### 3.7 依赖注入设计缺失 🟡 中等

设计中未说明如何解耦各层依赖。

**建议补充：**

```python
# common/container.py
from dependency_injector import containers, providers

class Container(containers.DeclarativeContainer):
    """依赖注入容器"""

    config = providers.Configuration()

    # 数据层
    tq_client = providers.Singleton(
        TqSdkClient,
        run_mode=config.run_mode
    )

    option_fetcher = providers.Factory(
        OptionQuoteFetcher,
        client=tq_client
    )

    # 业务层
    analyzer = providers.Factory(
        OptionAnalyzer,
        config=config.analyzer
    )

    # 交易层
    executor = providers.Factory(
        OrderExecutor,
        client=tq_client,
        risk_checker=providers.Factory(RiskChecker)
    )
```

### 3.8 迁移步骤需要更具体 🟡 中等

每个阶段缺少具体任务清单。

**建议补充阶段 1 详细步骤：**

```
阶段 1：创建新目录结构，提取公共模块

任务清单：
□ 1.1 创建目录结构
    □ 1.1.1 创建 common/ 目录
    □ 1.1.2 创建 __init__.py 文件
    □ 1.1.3 添加 pyproject.toml

□ 1.2 提取 config.py
    □ 1.2.1 整理各文件中的 RUN_MODE 定义
    □ 1.2.2 整理账户配置（脱敏处理）
    □ 1.2.3 创建 Config 类
    □ 1.2.4 编写单元测试

□ 1.3 提取 excel_io.py
    □ 1.3.1 整理 Excel 读写函数
    □ 1.3.2 创建 ExcelWriter/ExcelReader 类
    □ 1.3.3 统一列宽调整逻辑
    □ 1.3.4 编写单元测试

□ 1.4 提取 logger.py
    □ 1.4.1 整理 UnifiedLogger 用法
    □ 1.4.2 创建结构化日志函数
    □ 1.4.3 添加日志级别控制

□ 1.5 验证
    □ 1.5.1 公共模块单元测试通过
    □ 1.5.2 现有脚本仍可正常运行
```

### 3.9 性能考虑缺失 🟡 中等

大数据量场景（数万合约行情获取）需要性能优化设计。

**建议补充：**

```python
# data/option_quotes.py
class OptionQuoteFetcher:

    async def get_option_quotes_batch(self, symbols: List[str],
                                       batch_size: int = 200,
                                       max_concurrent: int = 5) -> Dict:
        """并发获取行情，控制并发数"""
        semaphore = asyncio.Semaphore(max_concurrent)

        async def fetch_batch(batch):
            async with semaphore:
                return await self._fetch_batch(batch)

        tasks = [fetch_batch(symbols[i:i+batch_size])
                 for i in range(0, len(symbols), batch_size)]
        results = await asyncio.gather(*tasks, return_exceptions=True)
        return self._merge_results(results)
```

### 3.10 风险缓解措施不够具体 🟡 中等

当前风险缓解过于笼统。

**建议补充：**

| 风险 | 可能性 | 影响 | 缓解措施 | 责任人 |
|------|--------|------|----------|--------|
| 重构过程中破坏现有功能 | 高 | 高 | 1. 每阶段运行完整测试<br>2. 保留旧文件备份<br>3. 灰度发布 | 开发者 |
| 大文件拆分逻辑遗漏 | 中 | 高 | 1. 先写测试覆盖核心逻辑<br>2. 代码审查<br>3. 对比新旧输出 | 开发者 |
| API 超时/连接失败 | 高 | 高 | 1. 断点续传机制<br>2. 自动重连<br>3. 告警通知 | 运维 |
| 配置泄露（账户密码） | 低 | 极高 | 1. 环境变量存储敏感信息<br>2. 配置文件不入库<br>3. 定期轮换密码 | 安全 |

---

## 四、设计建议

### 4.1 建议增加的模块

```
wisecoin-options-free/
├── ...
├── common/
│   ├── exceptions.py       # 新增：统一异常定义
│   ├── error_handler.py    # 新增：错误处理
│   ├── container.py        # 新增：依赖注入
│   └── metrics.py          # 新增：指标监控
├── ...
```

### 4.2 建议修改的设计

**数据层增加缓存：**

```python
# data/cache.py
class QuoteCache:
    """行情缓存，减少重复请求"""

    def __init__(self, ttl_seconds: int = 60):
        self._cache = {}
        self._ttl = ttl_seconds

    def get(self, symbol: str) -> Optional[Quote]:
        if symbol in self._cache:
            data, timestamp = self._cache[symbol]
            if time.time() - timestamp < self._ttl:
                return data
        return None

    def set(self, symbol: str, quote: Quote):
        self._cache[symbol] = (quote, time.time())
```

### 4.3 建议补充的文档

1. **API 设计文档** - 各模块接口详细定义
2. **数据字典** - 输入输出文件格式说明
3. **部署文档** - 生产环境部署指南
4. **故障处理手册** - 常见问题排查

---

## 五、总结

### 5.1 必须修改（阻塞发布）

| 编号 | 问题 | 严重程度 |
|------|------|----------|
| 3.1 | 异步处理策略缺失 | 🔴 严重 |
| 3.4 | 错误处理策略缺失 | 🔴 严重 |

### 5.2 建议修改（影响质量）

| 编号 | 问题 | 严重程度 |
|------|------|----------|
| 3.2 | 数据模型设计不完整 | 🟡 中等 |
| 3.3 | 配置管理设计需要细化 | 🟡 中等 |
| 3.5 | 日志与监控设计不足 | 🟡 中等 |
| 3.6 | 测试策略需要细化 | 🟡 中等 |
| 3.7 | 依赖注入设计缺失 | 🟡 中等 |
| 3.8 | 迁移步骤需要更具体 | 🟡 中等 |
| 3.9 | 性能考虑缺失 | 🟡 中等 |
| 3.10 | 风险缓解措施不够具体 | 🟡 中等 |

### 5.3 审查结论

**建议：** 修改后通过

设计方向正确，但需要补充异步处理和错误处理两个关键设计后再进入实施阶段。

---

*审查完毕*